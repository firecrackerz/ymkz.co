import styled from 'styled-components'

const Name = styled.p`
  color: var(--nord2);
  font-size: 8.5rem;
  font-weight: 900;
  @media (max-width: 768px) {
    font-size: 4rem;
  }
`

export default Name
