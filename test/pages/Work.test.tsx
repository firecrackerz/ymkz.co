import { shallow } from 'enzyme'
import * as React from 'react'
import Work from 'src/pages/Work'

describe('src/pages/Work.tsx', () => {
  test('Rendered <Work />', () => {
    shallow(<Work />)
  })
})
